{
  "name": "exercise-setup",
  "version": "1.0.0",
  "scripts": {
    "build:css": "sass src/scss/style.scss dist/style.css && sass --style compressed src/scss/style.scss dist/style.min.css",
    "build:js": "babel src/js/script.js -o dist/script.js && terser dist/script.js -o dist/script.min.js -c -m",
    "build": "npm run build:css && npm run build:js",
    "watch:css": "sass --watch src/scss/style.scss:dist/style.css",
    "watch:js": "nodemon --watch src/js/script.js --exec \"npm run build:js\"",
    "watch": "run-p watch:*"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.0",
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "cpx": "^1.2.1",
    "nodemon": "^3.1.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.93.0",
    "terser": "^5.30.0"
  },
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead"
  ]
}
